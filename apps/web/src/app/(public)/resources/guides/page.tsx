'use client';
import { useMemo, useState } from 'react';
import Link from 'next/link';
import { guides } from '@/components/public/site-data';
export default function Guides() { const [search, setSearch] = useState(''); const [region, setRegion] = useState('all'); const [topic, setTopic] = useState('all'); const visible = useMemo(() => guides.filter(g => (!search || `${g.title} ${g.summary} ${g.topic}`.toLowerCase().includes(search.toLowerCase())) && (region === 'all' || g.region === region) && (topic === 'all' || g.topic === topic)), [search, region, topic]); return <div className="bg-white"><section className="border-b border-slate-200 bg-slate-50"><div className="mx-auto max-w-7xl px-5 py-14 sm:px-8 lg:py-20"><p className="text-xs font-bold tracking-[.16em] text-blue-800">CAMPUSOS RESOURCE LIBRARY</p><h1 className="mt-3 max-w-3xl text-4xl font-semibold tracking-tight sm:text-5xl">Guides for modern higher-education operations</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600">Practical guidance for selecting, implementing and operating connected academic, administrative and student systems.</p></div></section><main id="main-content" className="mx-auto max-w-7xl px-5 py-12 sm:px-8"><div className="grid gap-3 rounded-lg border border-slate-200 bg-slate-50 p-4 md:grid-cols-3"><input aria-label="Search guides" value={search} onChange={e => setSearch(e.target.value)} placeholder="Search guides" className="min-h-11 rounded-md border border-slate-300 bg-white px-3 text-sm"/><select aria-label="Filter by region" value={region} onChange={e=>setRegion(e.target.value)} className="min-h-11 rounded-md border border-slate-300 bg-white px-3 text-sm"><option value="all">All regions</option><option value="us">United States</option><option value="in">India</option></select><select aria-label="Filter by topic" value={topic} onChange={e=>setTopic(e.target.value)} className="min-h-11 rounded-md border border-slate-300 bg-white px-3 text-sm"><option value="all">All topics</option>{Array.from(new Set(guides.map(g=>g.topic))).map(x=><option key={x}>{x}</option>)}</select></div><div className="mt-7 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{visible.map(g=><article key={g.slug} className="rounded-lg border border-slate-200 p-5"><p className="text-xs font-semibold text-blue-800">{g.region === 'us' ? 'UNITED STATES' : 'INDIA'} · {g.topic}</p><h2 className="mt-2 text-xl font-semibold">{g.title}</h2><p className="mt-3 text-sm text-slate-600">{g.summary}</p><p className="mt-4 text-xs text-slate-500">For {g.audience} · {g.time}</p><div className="mt-5 flex gap-4"><Link href={`/resources/guides/${g.slug}`} className="text-sm font-semibold text-blue-800">Open guide</Link><a href={`/api/public/guides/${g.slug}/download`} className="text-sm font-semibold text-blue-800">Download guide</a></div></article>)}</div>{!visible.length && <p className="py-12 text-center text-slate-600">No guides match these filters.</p>}</main></div>; }
